/**
 *  @packageDocumentation
 *  @hidden
 */

const MAIN = `/`;
const COUNTER = `/counter`;
const TODOS = `/todos`;
const NASA_APOD = `/nasa-apod`;
const FORM = `/form`;
const UPLOADER = '/uploader';

export default { MAIN, COUNTER, TODOS, NASA_APOD, FORM, UPLOADER };
